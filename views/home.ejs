<%- include("partials/header") -%>

<h1>HOME 🏡</h1>

<p><%= homecontent %> </p>

<% post.forEach(function(element){ %>
    <h1><%= element.title %></h1>
    <% let a = element.title;%>
    <p><%= element.content.substring(0,100) + "..." %> <a href="/post/<%=element.title%>">Read More</a> </p>
<% }); %>


<%- include("partials/footer") -%>
